import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.r0b3rt1n.listadecompras',
  appName: 'Lista de Compras',
  webDir: 'dist'
};

export default config;
